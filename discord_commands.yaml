- name: commands
  description: Shows available commands.


- name: fix
  description: Corrects message you input. Currently doesn't support emotes not part of native keyboards.
  options:
    - name: message
      description: The message to fix.
      type: 3
      required: true

- name: secret
  description: Try and see!
  options:
    - name: message
      description: The message to fix.
      type: 3
      required: true